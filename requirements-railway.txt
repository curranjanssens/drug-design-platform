# Drug Design Platform - Railway Dependencies
# RDKit is installed via conda in Dockerfile
# Removed: openbabel (not essential), torch (too large for Railway)

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# Async HTTP
aiohttp>=3.9.0
httpx>=0.25.0

# Database (optional - using in-memory for now)
sqlalchemy>=2.0.0

# Scientific Computing (lighter alternatives)
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.1.0
scikit-learn>=1.3.0

# Anthropic API
anthropic>=0.7.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0
requests>=2.31.0
loguru>=0.7.0
